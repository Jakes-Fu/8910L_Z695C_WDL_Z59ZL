0 所有的命名以“从简”原则。
1 文件名采用小写字母，每两个修饰字段之间用下划线相连。
2 因为中文输入的特殊性，所以分成中文与外文两种，目前外文只有拼写类的。
3 中文输入法一律用ch标识。
4 所有的文件前缀用spim标识。
5 代码的命名按照命名规范。
6 所有的对外函数名必须是自注释的。
7 对外函数名用SPIM_开头。
99 示例：
文件名，spim_xx_yy.c xx为语言标识(可略)，yy为输入法类型标识
函数名，SPIM_XxYyZz()